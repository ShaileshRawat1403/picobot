#!/usr/bin/env bash
# Launch Pico's local browser workbench using this repository's profile.
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$repo_root"

exec python3 -m picobot web \
  --host 127.0.0.1 \
  --port 18791 \
  --config .picobot/config.json
